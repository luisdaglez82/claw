 This is an activation atlas it Gives us a glimpse into the high dimensional embedding Spaces Modern ai models used to organize and make sense of the world the first model to really see the world like this Alex was published in Twenty twelve in an eight page paper that shocked the computer vision community by showing that an old ai día would work a well win scale.

The paper author

massively scale up this idea again to create chat this video is Sponsored by if you look under the hood of chat g you won't find any signs of intelligence instead you'll find layer after layer of compute blocks called transformers this is what the t and g p. T. Stands for. Each transformer performs a set of fixed matrix operations on an input matrix of data and typically returns an output matrix of the same size to figure out what it's going to say next chat gbt breaks apart when you ask it into words and word fragments maps each of these to a vectors all of these vector together into a matrix this matrix is then passed into the first transformer block which returns a new matrix of the same size.

This Operation is then repeated again and again ninety six times in chat tpt three point five and reportedly a hundred and Twenty times in chat gpt four now here's the absurd part with a few caveats the next word or word fragment that chat gpt says back to you es literally just the last column of its final output matrix mapped from a vector back to text.

To formulate a full response this new word a word fragment is dependent to the end of the original out and this new slightly longer text fed back into the input of chat chip p. This process is repeated again and again with one new column added to the input matrix each time until the models output returns to special stop word fragment.

And that is it one matrix multiply after another chat slowly give it into the out return where is the intelligence how is it that these a hundred or so blocks of dumb compute are able to write essays translate language sumarize books solve math problems explain complex concepts or even at the next line of this script.

The okay, pretty good but not quite what i wanted to say next the Alex net paper is significant because it marks the first time we really see Layers of computer block like this learning to do unbelievable things an ai tipping point towards high performance and scale and away from explainability. Gpt es train to predict the next a word fragment given some text Alex that is trained to predict el label given an image the input image to Alex n is represented as a three dimensional matrix tensor of RGB Intensity values and the output is a single vector of length one thousand entry correspond to Alex that's predicted probability that the input image belongs to one of the a thousand classes in the image that data set.

Things like tabi cats German shepherds hotdogs toasters and aircraft carriers just like chat today Alex was somehow magically able to map the inputs we give it into the outputs we wanted using layer after layer of compute block after training on a large data set one nice thing about vision models however is that it's easier to poke around under the hood.

Some idea what the model has learned one of the first under the hood insights that ksky suits in Hinton show in the Alex net paper is that the model has learned some very interesting visual patterns in its first layer the first five Layers of Alex that are all con blocks first developed in the late nineteen eighties to classify handwritten digits and can be understood as a special case of the transformer blocks in chat gpt and other large language models.

En con block the input image tensor es transform slid a much Weight values across the image location computing the do product between the image and nel here its to think of the do product is a similarity the more similar of the image in colonel r. The higher the resulting dot product will be Alex net uses ninety six individual colonels in its first layer each of dimension eleven by eleven by three.

So convenient we visualize them as little RGB images these images give us a nice idea how the first layer of Alex ne see the image the upper colonels in this figure show where Alex has clearly learned to detect edges o rapid changes from light to dark at various angles images with similar patterns will with these where Alex has learned to detect blobs of various colors these colonels are all initialized as random numbers.

And the patterns we're looking at are completely learned from data sliding each of our ninety six colonels over the input image and computing the dot product at each location produces a new set of ninety six matrices sometimes called activation maps conveniently we can view these as images as well the activation maps show us which parts of an image if any match a given colonel well if i hold up something visually similar to a given colonel we see high activation in that part of the activation map.

It goes away when i rotate the pattern by ninety degrees the image and kernel are no longer aligned you can also see various activation maps picking up edges and other low-level features in our image of course finding edges and color blobs in images is still hugely removed from recognizing complex concepts like German shepherds or aircraft carriers what's as astounding about deep neural networks like Alex ne and chat gpt is that from here all we do is repeat the same Operation again.

Just with a different set of learned waits for Alex ne this means that these nineties activation maps are staed together into a tenser that become the input to the exact same type of convolutional compute block the second overall layer in the model we can make our activations easier to see by removing the values close to zero unfortunately in our second layer we can't learn much by simply visualizing the Weight values in the colonels themselves.

The first issue is that we just can't see enough colors the depth of the colonel has to match the depth of the incoming data in the first layer of Alex net the depth of the incoming data is just three because the model takes in color images with red green and blue color channels however since the first layer compute ninety six separate activation maps the computation in the second layer of Alex that is like processing images with ninety six separate color channels.

The second factor that makes what's happening in the second layer of Alex not more difficult to visualize es that the dot products are really taking weighted combinations of the computations in the first layer we need some way to visualize how the Layers are working together a simple way to see what's going on es to try to find parts of various images that strongly activate the outputs of the second layer por example this activation map appear to be putting together edge detectors to form basic corners.

Remarkably we move deeper into Alex ne strong activations correspond a higher and higher level concepts by the time we reach the fifth layer we have activation maps that respond very strongly to faces and other high level concepts en what's incredible here es that no one explicitly told Alex net what a face is all Alex had to learn from where the images and labels and the image and that data set which does not contain a person o a face class.

Alex net was able to learn completely on its own both that faces are important and how to recognize them to better understand what a given colonel and Alex ne has learned we can also look at the examples in the training data set that give the highest activation values for that colonel for our face colonel not surprisingly we find examples that contain people finally there's this really interesting technique called feature visualization where we can generate synthetic images that are optimized to maximize a given activation.

The synthetic images give us another way to see specific activation layer is looking for by the time you reach the final layer of Alex net arm has been processed into a vector of length four thousand and ninety six the final layer performs one last matrix computation on this vector to create a final output vector of length one thousand with one entry for each of the classes in the image that dataset kski suga in Hinton noticed that the second to last layer vector demonstrated some very interesting properties.

One way to think about this vector is as a point in four thousand and ninety six dimensional space each imagery pas into the model is effectively mapp to a point in this space all we have to do is just stop one layer early and grab this vector just as we can measure the distance between two points and 2d space we can also measure the distance between points or images in this high-dimensional space hinton's team ran a simple experiment where they took a test image in the image that data set.

Computer its corresponding vector and then search for the other images in the image that data set that were closest or the nearest neighbors to the test image in this high dimensional space remarkably the nearest neighbor images showed highly similar concepts to the test images en figure four from the Alex net paper we see an example where an elephant test image yields nearest neighbors that are all elephants what's interesting here too is that the pixel values themselves between these images are very different.

Alex net really has learned high dimensional representations of data where similar concepts are physically close this high-dimensional space is often called a latent or bedding space in the years following the Alex net paper it was shown that not only distance but directionality in some of these embedding Spaces es meaningful the demos you see where faces are age or gender shifted.

To a vector space and l. Moving this in the age direction in that space and ma the modifi vector back to image before we get into activation alice which give us an amazing way to visualize these space please take a moment to consider if this video sponsor is something that you or someone in your life would enjoy i was genuinely really excited to work with this company they make incredibly thoughtful educational products.

Des helping me make more of these videos this video sponsor es kiwi they make these fun and super well designed educational for kids of all ages they have nine different Monthly subscription lines to choose from focus on different areas of ste and you can also buy individuals which are great for trying out kiw ko and make amazing gifts Growing up i was constantly building here i am building a Tower outside my house to my second story bedroom.

I was obsessed with Electronics and would've absolutely loved projects like this pencil from the create line which is focused on Science and Engineering i really believe that this type of handson self-driven learning is magical when i really think about my own education it's the times that I've been fully absorbed in projects like this that i learned the most and now that I'm a dad i really want my kids to have the same kind of experiences kiwi co really does an amazing job boxing up start to finish projects like this.

Just got it includes the special specifically de help ways of grasping you can see her insisting that she gets to bring them in the car us huge thanks to this video use the discount code for fifty percent off your month of a subscription now back to Alex there's some really amazing work that combines the synthetic images that maximize a given set of activation.

With a two-dimensional projection or flattening out of the embedding space to make these incredible visualizations called activation atlas neighbors on the activation atlas are generally close in the embedding space and show similar concepts the model has learned we're getting a peak into how deep ne networks organize the visual world looking at the synthetic images that most activate Neighborhoods of neurons we can visually walk through the embedding space of the model.

Seeing it make smooth visual transitions from concepts like zebras to tigers to leopards to rabbits moving to the middle Layers of the model we can see less fully formed but still meaningful concepts moving along this path amazingly correlates with the number and size of pieces of fruit in an image the same principle applies in large language models words and word fragments are mapped to vectors in an embedding space.

Similar meetings are close to each other and the directions in the embedding space are sometimes semantically meaningful there's some incredible very recent work from the that sets of activations can be mapped to concepts in language these us understand and can be used to modify model Behavior after clamping a set of activations that correspond to the concept golden gate bridge to a high value.

Begin to identify itself as the golden gate bridge Alex net won the image large scale visual recognition challenge a wide thousand twelve the run the winning that the you expect to find in an Intelligent system the two thousand eleven winner used a complex set of very different algorithms. With an a called sift which is composed of specialized image techniques developed by experts over many years of research Alex ne en contrast is an implementation of a much older ai idea an artificial network where the Behavior of the a from the Operation between the data and a set of waits was originally proposed by mccoll and pits in the nineteen forties as a dramatically oversimplified model of the neurons in our brain.

The second half of the you'll find a multi per the percept is a learning algorithm and Physical machine from the nineteen fifties that uses mc pi neurons and can learn to perform basic shape recognition Tasks back in the nineteen eighties a younger Jeff Hinton and his collaborators at carnegie Mellon.

Layers of these per using a multiva calculus techn called back propagation these models were a couple Layers deep and remarkably pretty good at driving cars in the nineteen nineties y on la coon now chief ai scientist at meta was able to train five layer deep models to recognize handwritten digits despite the intermittent successes of artificial neural networks over the years this approach was hardly the accepted way to do ai.

Right up into the publication of Alex ne if this was obviously the way to build intelligence systems we would've done it decades earlier es Ian good fellow rights in it excellent deep learning book at this point deep networks were generally believed be very difficult to train we now know that algorithms that have existed since the nineteen eighties were quite well but this was not a parent circuit two thousand and six.

The issue is perhaps simply that these algorithms were too computationally costly to allow much experimentation with the hardware available at the time the key difference in two thousand twelve was simply scale of data en scale of compute the image that data set was the largest label data set of its kind to date.

Thousand

thousand fold to around Sixty million parameters today chat gpt has well over a trillion parameters making it over ten thousand times larger than Alex net. This boggling scale is the Hallmark of this third wave of ai we find ourselves in today driving the performance and the fundamental difficulty in understanding how these models are able to do what they do.

It's amazing that we can figure out that Alex that learns representations of faces and that large language models learn representations of concepts like the golden gate bridge but there are many many more concepts these models learned that we don't even have words for activation atlases are beautiful and fascinating.

But very low dimensional projections of very high dimensional Spaces where our spacial reasoning abilities often apart it's notoriously difficult to predict where ai will go next almost no one expected the neural networks of the eighties and nineties scaled up by three or four orders of magnitude to Yield Alex net and it was almost impossible to predict that a generalization of the compute blocks and Alex net scaled up by four of magnitude would Yield chat j.

PT. Maybe the next ai breakthrough is just another three to orders of magnitude scale away or maybe some mostly forgotten approach de ai will resurface as Alex that did in two thousand twelve we'll have to wait

describing the compute blocks as dumb. Nature

it's a way to emphas the power of the a.

